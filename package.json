{
  "name": "hardware-store-management-system",
  "version": "1.0.0",
  "scripts": {
    "build:all": "npm run build --prefix backend && npm run build --prefix Frontend",
    "desktop": "npm run build:all && npm start --prefix electron",
    "desktop:dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix Frontend\" \"wait-on http://localhost:5173 && npm run dev --prefix electron\"",
    "package": "npm run build:all && npm run dist --prefix electron"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0",
    "cross-env": "^7.0.3"
  }
}
